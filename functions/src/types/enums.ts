export enum UserRole {
  TEACHER = "teacher",
  STUDENT = "student",
}

export enum PublishStatus {
  PENDING = "PENDING",
  PUBLISHED = "PUBLISHED",
  FAILED = "FAILED",
}

export enum ClassroomItemType {
  COURSE_WORK_MATERIAL = "COURSE_WORK_MATERIAL",
  COURSE_WORK = "COURSE_WORK",
}

export enum ClassroomState {
  PUBLISHED = "PUBLISHED",
  DRAFT = "DRAFT",
}

export enum CourseState {
  ACTIVE = "ACTIVE",
  ARCHIVED = "ARCHIVED",
  PROVISIONED = "PROVISIONED",
  DECLINED = "DECLINED",
}

export enum HealthStatus {
  OK = "ok",
}
